{
  "name": "account",
  "version": "0.1.0",
  "private": true,
  "description": "Account management plugin (change username/password) for Claude Code UI.",
  "license": "AGPL-3.0-or-later",
  "type": "module",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --target=es2020 --outfile=dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0",
    "typescript": "^5.5.0"
  }
}
